
// maybe we can define `class` as well.
// but then we need to decide initialization and clean up of objects
class something {
    a;
    b = 1;
    
    function getA() {
        return this.a;
    }
}

