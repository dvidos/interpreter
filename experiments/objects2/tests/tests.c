

void test_investigate_types_as_first_class_citizens() {
    // given that a type can be thought of as an object,
    // what can we do with it? can we clone and modify it?
    // can we make it to_string()?
    // can we pass it to functions and have them do something with it?
    // all functions for manipulating an object should work for a type as well.
}

void run_objects_tests() {

    void test_coordinates_class();
    test_coordinates_class();

    void test_callables_class();
    test_callables_class();

    test_investigate_types_as_first_class_citizens();
}
