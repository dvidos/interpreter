#include <stdio.h>
#include "tests/tests.h"



int main() {
    // int=4, long=8
    // printf("sizeof(int)=%ld, sizeof(long)=%ld\n", sizeof(int), sizeof(long));
    
    run_objects1_tests();
    return 0;
}
